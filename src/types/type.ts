export type _CartItem = {
    id: string;
    name: string;
    price: number;
    currency: string;
    quantity: number;
}

export type _Product = {
    id: string;
    name: string;
    price: number;
    currency: string;
    imageSrc: string;
    imageAlt: string;
}